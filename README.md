# Arduino-Digital-Analog-Sensors
This project demonstrates the difference between digital and analog sensors using Arduino.  A PIR motion sensor is used as a digital input, and a potentiometer is used as an analog input to control LED brightness.
